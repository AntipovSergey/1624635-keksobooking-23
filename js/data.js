const SIMILAR_ADVERTISEMENTS = 10;

const PHOTO_WIDTH = 45;

const PHOTO_HEIGHT = 40;

const MIN_NAME_LENGTH = 30;

const MAX_NAME_LENGTH = 100;

const MAX_PRICE_LENGTH = 1000000;

const PRICE_VALUES = {
  bungalow: 0,
  flat: 1000,
  hotel: 3000,
  house: 5000,
  palace: 10000,
};

const NUMBER_OF_ROOMS = {
  oneRoom: '1',
  twoRooms: '2',
  threeRooms: '3',
  hundredRooms: '100',
};

const LAT_LANG_DEFAULT = {
  lat:35.680929,
  lng:139.768601,
};

const ALERT_SHOW_TIME = 5;

export {SIMILAR_ADVERTISEMENTS, PHOTO_WIDTH, PHOTO_HEIGHT, MIN_NAME_LENGTH, MAX_NAME_LENGTH, MAX_PRICE_LENGTH, PRICE_VALUES, NUMBER_OF_ROOMS, LAT_LANG_DEFAULT, ALERT_SHOW_TIME};
